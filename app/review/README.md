# Candidate Review

`/review` is the human curation layer for discovered cars. It reads `data/worker-candidates.json`, links to source adverts, and allows browser-local Add / Reject / Check later decisions while persistent publishing is being wired.
